import React from 'react';
import { View, Text, TouchableOpacity } from 'react-native';

const Button = () => {
  return (
    <TouchableOpacity>
      <Text></Text>
    </TouchableOpacity>
  )
}

const styles = {
  buttonStyle : {
    borderWidth : 1,
    borderColor : "#007aff",
    borderRadius : 10
  }
}

export default Button;
